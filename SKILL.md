---
name: leetcode-review-report
description: 审查用户随 LeetCode 题号提交的完整解题代码，持续复审更正版本，并仅在确认正确后自动生成中文 Markdown 结题报告；支持配置报告输出目录。用于题号加代码的解法审查，不用于只询问题意或通用算法概念。
metadata:
  short-description: 审查 LeetCode 解法并在正确后生成报告
---

# LeetCode 解法审查与结题报告

## 适用边界

- 用户给出 LeetCode 题号和自己的完整解题代码时使用本流程。
- 先读取当前项目中适用的 `AGENTS.md`，本 skill 与项目规则冲突时遵守更具体或更严格的规则。
- 以用户在对话中提交的代码为审查对象；除非用户明确指出源码文件，否则不要默认在项目里搜索或替换其解题代码。
- 不要因用户要求“生成报告”而跳过正确性审查。代码尚未确认正确时，不得生成结题报告。

## 报告目录配置

按以下优先级确定报告目录，命中后停止继续查找：

1. 用户在当前请求中明确指定的目录。
2. 项目根目录 `.codex/leetcode-review.json` 中的 `reportDirectory`。
3. 环境变量 `LEETCODE_REPORT_DIR`。
4. 项目根目录下的 `reports/`。

项目配置示例：

```json
{
  "reportDirectory": "reports/leetcode"
}
```

- 将相对路径按项目根目录解析；绝对路径保持不变。
- 项目根目录优先取当前工作区根目录；若存在更近且适用的 `AGENTS.md`，使用其所约束的项目根目录。
- 在写入前解析并展示最终绝对路径。若选定目录无效、无法创建或不可写，说明原因并停止，不要静默改写到其他目录。
- 不要仅为使用默认值而创建配置文件；用户要求持久配置时再创建或修改它。

## 审查流程

1. 从对话取得题号、完整代码、编程语言以及用户说明。信息不足时，只询问会阻止正确性判断的关键内容。
2. 根据题号核对题目标题、链接、难度、示例和约束。优先使用 LeetCode 官方题面；无法访问时明确说明，并基于用户提供的题面或可确认的信息继续。
3. 先理解用户实现的核心思路和不变量，再检查：
   - 算法逻辑与正确性；
   - 初始化、循环边界、终止条件和答案更新；
   - 空输入、最小/最大规模、重复值、极值与溢出等特殊情况；
   - 时间复杂度、空间复杂度和是否满足约束；
   - 可读性问题，但不要把风格偏好误判为正确性缺陷。
4. 在环境和代码完整度允许时，使用与风险相称的验证：官方示例、针对性边界用例、编译运行，以及适合时的小规模暴力对拍。测试只能增加证据，不能替代正确性推理；准确区分静态分析、编译、运行、对拍和平台提交结果。
5. 若发现问题：
   - 给出能触发问题的输入或精确执行路径；
   - 解释根因，并提出具体、可执行、尽量小的修改建议；
   - 未经用户要求不要直接修改其源码；
   - 等用户提交修正版后，从步骤 2 继续复审；
   - 此阶段不得创建结题报告。
6. 只有在题目约束已经核对、完整代码已审查、未发现反例，并有足够的正确性论证时，才将状态判定为“已确认正确”。无法运行代码不必然阻止静态证明，但必须如实说明未执行的验证，不能声称测试通过。

## 正确后的自动报告

一旦状态为“已确认正确”，无需用户再次提醒：

1. 读取 [报告规范](references/report-spec.md)。
2. 基于最终确认正确的用户代码生成中文 Markdown，不要擅自替换成另一套实现。
3. 创建已解析的报告目录，并以 `题号-中文题名-YYYY-MM-DD.md` 命名；题名不适合作为文件名时使用 `题号-YYYY-MM-DD.md`。移除 Windows 文件名非法字符以及末尾的空格或句点。
4. 若目标文件已存在，除非用户明确要求更新它，否则使用 `-2`、`-3` 等后缀避免覆盖。
5. 写入后重新读取文件，确认用户最终代码、必需章节和验证结论均存在。
6. 向用户说明审查结论、实际执行的验证，并返回报告的完整绝对路径。

## 停止条件

- 代码错误、代码不完整、题目版本无法确定且会影响结论，或缺少足以确认正确性的关键约束：停留在审查阶段，不生成报告。
- 用户明确只要审查且不要生成文件：遵从该请求，即使代码正确也不写报告。
- 用户明确要求修改源码时，修改仍须限于其授权范围；报告门槛保持不变。
